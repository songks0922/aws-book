spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
  h2:
    console:
      enabled: true
  profiles:
    include: oauth
  session:
    store-type: jdbc
